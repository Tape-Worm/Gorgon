<Window x:Class="Gorgon.UI.Wpf.DialogWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gorgon.UI.Wpf"
        mc:Ignorable="d"
        Title="Dialog" MinHeight="159" MinWidth="450" Width="auto" Height="auto"
        d:Width="450" d:Height="300"
        WindowStartupLocation="CenterScreen" UseLayoutRounding="True" SnapsToDevicePixels="True" SizeToContent="WidthAndHeight" ResizeMode="NoResize" Icon="pack://application:,,,/Gorgon.UI.Wpf;component/Gorgon2.ico" Loaded="Window_Loaded" >
    <Window.Background>
        <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.ControlColorKey}}"/>
    </Window.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image x:Name="ImageIcon" Grid.Column="0" VerticalAlignment="Top" Source="pack://application:,,,/Gorgon.UI.Wpf;component/Resources/Confirm_48x48.png" Stretch="Uniform" MaxWidth="48" MaxHeight="48"/>
            <TextBlock x:Name="LabelMessage" Grid.Column="1" VerticalAlignment="Top" MinHeight="90" Margin="3,3,3,3" TextWrapping="Wrap">Message</TextBlock>
        </Grid>
        <Grid Grid.Row="1">
            <Border x:Name="PanelButtons" Background="White" BorderBrush="Black" BorderThickness="0,1,0,0" >
                <DockPanel>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Visibility="Collapsed" x:Name="CheckToAll" Margin="3, 3, 3, 3" VerticalContentAlignment="Center">To All</CheckBox>
                        <ToggleButton Visibility="Collapsed" x:Name="CheckDetail" Margin="3, 3, 3, 3" MinWidth="69" MinHeight="25" VerticalContentAlignment="Center" Click="CheckDetail_Click">Details</ToggleButton>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Visibility="Collapsed" x:Name="ButtonOK" HorizontalAlignment="Right" MinWidth="69" MinHeight="25" Margin="3, 3, 3, 3" Click="ButtonOK_Click">OK</Button>
                        <Button Visibility="Collapsed" x:Name="ButtonYes" HorizontalAlignment="Right" MinWidth="69" MinHeight="25" Margin="3, 3, 3, 3" Click="ButtonYes_Click">Yes</Button>
                        <Button Visibility="Collapsed" x:Name="ButtonNo" HorizontalAlignment="Right" MinWidth="69" MinHeight="25" Margin="3, 3, 3, 3" Click="ButtonNo_Click">No</Button>
                        <Button Visibility="Collapsed" x:Name="ButtonCancel" HorizontalAlignment="Right" MinWidth="69" MinHeight="25" Margin="3, 3, 3, 3" Click="ButtonCancel_Click" IsCancel="True">Cancel</Button>
                    </StackPanel>
                </DockPanel>
            </Border>
        </Grid>
        <Grid Grid.Row="2" MinHeight="200" x:Name="PanelDetails" Visibility="Collapsed">
            <TextBox x:Name="TextDetails" FontFamily="Consolas" FontSize="12" Background="#FFE0E0DC" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible" Width="{Binding ActualWidth, ElementName=PanelDetails}" IsReadOnly="True"/>
        </Grid>
    </Grid>
</Window>
